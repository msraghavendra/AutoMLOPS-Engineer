# Pyrefly configuration — points to the project virtual environment
# so that sqlalchemy, pydantic, fastapi etc. are resolved correctly.
python-interpreter = ".venv/Scripts/python.exe"
site-package-path = [".venv/Lib/site-packages"]
